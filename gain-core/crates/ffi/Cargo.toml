[package]
name = "ffi"
version = "0.1.0"
edition = "2021"

[lib]
name = "gain_stage_ffi"
crate-type = ["cdylib"]

[dependencies]
gain-api = { path = "../gain-api" }

[dev-dependencies]
tempfile = "3"
